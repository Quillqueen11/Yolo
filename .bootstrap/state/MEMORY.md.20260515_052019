# MEMORY — Long-term memory for Quill

## Identity & Core Preferences
- Name: Quill (AI assistant), User: Andry
- Communication: Indonesian, direct, concise (≤3 sentences), no filler, answer first
- Style: Super smart, efficient | Timezone: Asia/Jakarta
- Autonomy: ✅ Full approval to write/rewrite any code independently
- User sends requests in **layers** (new request while previous pipeline running) → send quick ack first, offer re-run if interrupted
- User prefers **structured Markdown tables** for reports (metrics, status, comparisons)

## Technical Configuration
- API: Sumopod (`https://ai.sumopod.com`, model `glm-5.1`)
- Tavily: ✅, Pexels: ✅, Pixabay: ✅
- Python: `/app/venv`, Node: v25.6.0

## WordPress

### investor-idn.com
- URL: `https://investor-idn.com`, User: andryantoen
- Categories: Trending(1) + 1 of (Company-77, Market-76, Event-80, Intl-79, Sustain-78)
- Skip: structured warrant articles
- Tracked emiten: APIC, ASII, BEI, BEKS, BKSL, BOAT, BULL, BUMI, COIN, EMAS, HILL, IBST, INET, NPGF, SMIL, SUPR

### aksarabaru.com
- URL: `https://aksarabaru.com`, User: Quill Queen (ID 12), Password: Langsung123
- 7 categories: Bisnis(3), Hiburan(6), Otomotif(7), Kuliner(5), Sosial Media(4), Olahraga(8), Teknologi(10)
- Writing: 200 words, Gen Z journalist, conversational ID — lihat `GENZ_WRITING_GUIDE.md`
- Rotation: HOUR % 7 | Format: "Jakarta, aksarabaru.com – " di awal (pakai ndash)
- Larangan: Jangan ulang format pembukaan di paragraf2+; Jangan mulai 2+ paragraf dgn kata sama
- **KONTEN NEGATIF DILARANG**: cerai, selingkuh, isu sensasi artis, politik, kriminal, perang, isu sensitif (Palestine-Israel, Papua, dll)
- **Content filter workaround**: pipeline may select criminal/sensitive topics → manually reject & replace; fallback: fetch from cleaner sources (Google News direct)
- SEO: excerpt auto, tags auto dari category | Dedup: Jaccard 0.30 + stem
- Author: Quill Queen (ID 12) | No featured image

## IDX API & Cloudflare
- Base: `https://www.idx.co.id/primary/`
- Auth: `curl_cffi` impersonate="chrome"; seed main page first
- Filter: EXCLUDE routine; INCLUDE board changes, dividends, buybacks, PKPU

## Pipelines

### IDX Auto-News (investor-idn.com) — v4
Fetch 240 (3s) → Filter → Triple dedup → Parallel enrich (Tavily+Chroma) → Image (Pexels `src["large"]`→Pixabay) → **LLM write** (`qwenpaw agents chat`) → WP publish
- Writing: KPIG — lead angka → konteks → detail → profil emiten → prospek; KBBI/EYD, human tone
- **Title**: 3-layer LLM generation (main prompt → quality gate → `refine_title_with_llm()` post-process)
- **Image**: 3 keyword variants per article (type-specific → broad industry → neutral) + `used_image_hashes` state file for persistent dedup
- Author: Tiara Reca (ID 4), Categories: CATEGORY_MAP + Trending(1)
- Script: `scripts/auto_news_pipeline.py`
- Strip English: `strip_english()` removes EN before LLM input
- Fallback: template on LLM failure

### Aksarabaru Auto-News
- Scripts: `scripts/aksarabaru_pipeline.py` (fetch) → `scripts/aksara_publish.py` (publish via LLM)
- Dedup: title hash cache + WP existing check

## Subprocess LLM Access
```python
subprocess.run(["qwenpaw","agents","chat","--from-agent","default","--to-agent","default","--text",prompt], capture_output=True, text=True, timeout=120)
```
- Calls internal API (localhost:8088) → Sumopod
- Used by `auto_news_pipeline.py` v4 for KPIG writing + title refinement; fallback to template on failure

## Constraints
- curl_cffi: no `files=` for multipart → use `requests` for WP media upload
- Cloudflare: session management with curl_cffi, seed main page first
- Long pipelines: send quick acknowledgment first; if user stacks multiple commands, execute sequentially with status per step

## Cron Jobs (8, staggered)
| Job | Schedule |
|-----|----------|
| IDX Auto-News v4 | `0 6-22/2 * * *` |
| HEARTBEAT Daily Memory | `0 7 * * *` |
| HEARTBEAT Self-Improve | `10 */6 * * *` |
| Indexing Pipeline | `15 1,9,17 * * *` |
| Quill Auto-Update | `20 */12 * * *` |
| Aksarabaru Auto-News | `30 * * * *` |
| Foreign News Pipeline | `45 7,19 * * *` |
| Aksarabaru Indexing | `50 */4 * * *` |

## Writing Style Separation — KRITIS!

### investor-idn.com — Formal, KBBI/EYD, KPIG struktur
### aksarabaru.com — Gen Z, santai, inverted pyramid

**Jangan tertukar.** Formal ≠ Gen Z. Gen Z ≠ formal.

## Quality Fix Protocol
When user reports quality issues (titles, images, formatting):
1. Diagnose root cause in pipeline code first
2. Fix pipeline code for future articles
3. Also fix past articles manually (rate limit: ~1 article per 60s for LLM cooldown)
4. Use hash dedup for images across batches to guarantee uniqueness
